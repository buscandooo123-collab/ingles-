rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each user (identified by pattern hash) has their own document
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}
